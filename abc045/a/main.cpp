#include <bits/stdc++.h>
using namespace std;

// define
#define rep(i,n) for (int i = 0; i < (n); ++i)

int main() {
  int a,b,h;
  cin >> a >> b >> h;
  int area = 0;
  area = (a+b)*h/2;
  cout << area << endl;
  return 0;
}
