#include <bits/stdc++.h>
using namespace std;

// define
#define rep(i,n) for (int i = 0; i < (n); ++i)

int main() {
  string a,s,c;
  cin >> a >> s >> c;
  cout << 'A' + s.substr(0,1) + 'C' << endl;
  return 0;
}
